{
  "name": "@tic-tac-toe/web-vanilla",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "concurrently -k -s first \"yarn run build && yarn run serve\" \"cd ../web-test && yarn run cypress:run\""
  },
  "devDependencies": {
    "vite": "^2.5.4"
  },
  "dependencies": {
    "@tic-tac-toe/core": "^1.0.0"
  }
}
